import React from "react";
import ReactDOM from "react-dom/client";

const heading = <h1>MVVR Group of Companies</h1>

const route = ReactDOM.createRoot(document.getElementById("root"));

route.render(heading);